// array is an object and each entry is a name value pair with index as name and value as value
// we can loop through an array using for(var prop in arr);
// we should not use (for .. in ) iterator for arrays since arrays are objects.