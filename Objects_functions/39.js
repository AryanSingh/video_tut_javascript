// arguments is a special keyword. It means the parameter we are passing to a function.
// arguments hold whatever values we are passing to a function.
// we can call a function in javascript with wrong no. of arguments or no argument at all.
// if arguments are passed to function they are processed left to right.
// arguments list is arraylike. It looks and acts like array but does not have all the feature of an array.