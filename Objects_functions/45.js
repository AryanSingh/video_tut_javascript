// IIFE are safe because any outside code cannot interract with the code of the anonymous function.
// if we want to changge the global code using anonymous function we just have to pass
// the window object.